# =============================================================================
# Spisak migracija koje CI primenjuje na praznu Postgres bazu POSLE bootstrap-a.
# Redosled je bitan. Komentari (# ...) i prazni redovi se ignorišu.
#
# Namerno preskačemo:
#   - sql/migrations/add_loc_step4_pgcron.sql — zahteva `pg_cron` ekstenziju
#     koja nije dostupna van Supabase/self-hosted PG sa posebnom konfiguracijom.
#     Scheduler logika se smoke-testira na Supabase-u manuelno.
#   - sql/migrations/add_loc_module_step1_tables.sql — subset add_loc_module.sql;
#     idempotentne DDL naredbe bi prošle, ali je redundantno.
# =============================================================================

# Minimalni storage.* stub za CI (plain Postgres nema Supabase Storage šemu).
sql/ci/storage_stub.sql

sql/migrations/add_loc_module.sql
sql/migrations/add_loc_step2_ci_unique.sql
sql/migrations/add_loc_step3_cleanup.sql
sql/migrations/add_loc_step5_sync_rpcs.sql
sql/migrations/add_loc_v2_quantity.sql
sql/migrations/add_loc_v3_order_scope.sql
sql/migrations/add_loc_v4_drawing_no.sql

# Härd-1 (Sprint LOC-Härd-1): client_event_uuid idempotency + advisory lock +
# opcija B (INITIAL akumulacija) + parent_inactive + constraint_violation.
# Mora posle v4 jer prepiše loc_create_movement.
sql/migrations/harden_loc_create_movement_v5.sql

# Härd-2 (Sprint LOC-Härd-2): loc_can_create_movement() helper + RPC autorizacija.
# Dozvoljava admin/leadpm/pm/menadzment ili employee u Proizvodnji (dept 2),
# Montaži (dept 3) ili sub_dept "Magacin i logistika".
sql/migrations/harden_loc_create_movement_v5_roles.sql

# Härd-3 (Sprint LOC-Härd-3): operativna vidljivost — worker heartbeat tabela
# + DEAD_LETTER digest outbox + dispatch RPC-ovi. pg_cron schedule grana se
# preskače u CI-u (handler hvata undefined_table).
sql/migrations/add_loc_sync_health_monitor.sql

# Kadrovska DDL — preduslov za harden_kadr_* i pgTAP (absences, vacation_entitlements, …)
sql/migrations/add_kadrovska_module.sql
sql/migrations/add_kadrovska_phase1.sql
sql/migrations/add_attendance_grid.sql
sql/migrations/add_kadr_employee_extended.sql
# current_user_is_admin() — pre restrict_employee_pii_admin_only (guard na liniji 23)
sql/migrations/enable_user_roles_rls_proper.sql
sql/migrations/restrict_employee_pii_admin_only.sql
sql/migrations/add_kadr_work_type.sql

# Faza 2 security / lokacije — potrebno za pgTAP (has_edit_role, RLS, audit, HALA↔POLICA pravila)
sql/migrations/add_menadzment_full_edit_kadrovska.sql
# Kadrovska K5 + scope (posle current_user_is_admin); extend za manages_employee;
# harden za write RLS Sprint 2.2.
sql/migrations/add_rbac_managed_departments.sql
sql/migrations/add_kadr_vacation_requests.sql
sql/ci/kadrovska_grants.sql
sql/migrations/extend_kadr_managed_departments_scope.sql
sql/migrations/harden_kadr_menadzment_write_scope.sql
sql/migrations/add_kadr_dashboard_kpis_rpc.sql
sql/migrations/fix_kadr_dashboard_kpis_active_employees_company_wide.sql
sql/migrations/add_kadr_dashboard_mini_reports_rpc.sql
sql/migrations/add_kadr_notifications.sql
sql/migrations/add_kadr_dashboard_action_stack_rpc.sql
sql/migrations/refactor_managed_to_sub_department_ids.sql
sql/migrations/update_dashboard_rpcs_for_sub_dept_scope.sql
sql/migrations/fix_kadr_dashboard_active_employees_scope.sql
sql/migrations/fix_kadr_action_stack_missing_grid_hr_only.sql
sql/migrations/backfill_dusko_kostic_managed_sub_departments.sql
sql/migrations/backfill_zoran_jarakovic_managed_odrzavanje.sql
sql/migrations/backfill_nikola_ninkovic_masinska_obrada.sql
sql/migrations/harden_kadr_select_scope_menadzment.sql
sql/migrations/add_audit_log.sql
sql/migrations/add_audit_actor_attribution.sql
sql/migrations/add_loc_location_hierarchy_rules.sql
sql/migrations/add_loc_locations_audit.sql

# Održavanje (CMMS) — stek: modul, katalog mašina, odgovorni, lokacije, supertablica sredstava
# (bootstrap u `00_bootstrap.sql` daje `project_id` + stub `bigtehn_machines_cache` za CI)
sql/migrations/add_maintenance_module.sql

# Reversi — zavisi od loc_* + touch_updated_at() (add_maintenance_module)
sql/migrations/add_reversi_module.sql
sql/migrations/add_reversi_reversal_pdf_storage.sql
sql/migrations/add_reversi_cutting_tools.sql
# Scoped UNIQUE na loc_locations + rev_get_or_create_recipient_location UPSERT (pgTAP reversi cutting)
sql/migrations/loc_location_code_scope_unique_strip_prefix.sql
# BigTehn artikli mirror (R_Artikli + lookup tabele) — referenciraju rev_tools / rev_cutting_tool_catalog,
# pa mora posle add_reversi_cutting_tools.sql.
sql/migrations/add_bigtehn_artikli_cache.sql

# Reversi — kategorija na rev_tools (alat/odelo/oprema/zaštitna), za bulk import iz Excel-a
sql/migrations/add_reversi_tool_categories.sql

# Sastanci — baza + RLS hardening + prefs/draft/lock/RPC koje pokrivaju pgTAP testovi
sql/migrations/add_sastanci_module.sql
sql/migrations/harden_sastanci_rls_phase2.sql
sql/migrations/harden_sastanci_write_rls.sql
sql/migrations/add_sastanci_locked_guard.sql
sql/migrations/add_sastanci_notification_prefs.sql
sql/migrations/add_sast_pm_teme_draft.sql
sql/migrations/add_sast_draft_rls.sql
sql/migrations/add_sast_zakljucaj_rpc.sql
sql/migrations/add_sast_notification_dedup.sql
sql/migrations/add_sast_email_indexes.sql
sql/migrations/add_sast_dashboard_stats_rpc.sql
sql/migrations/extend_sastanci_tip_tematski_dnevni.sql
sql/migrations/add_sastanci_invite_clear_on_ucesnik_delete.sql

sql/migrations/add_maint_machines_catalog.sql
sql/migrations/add_maint_machine_responsible.sql
sql/migrations/add_maint_locations.sql
sql/migrations/add_maint_assets_supertable.sql
sql/migrations/add_maint_work_orders.sql
sql/migrations/link_maint_incidents_to_wo.sql
sql/migrations/extend_maint_incidents_assets.sql
sql/migrations/add_maint_documents.sql
sql/migrations/add_maint_inventory.sql
sql/migrations/add_maint_vehicle_details.sql
sql/migrations/add_maint_it_asset_details.sql
sql/migrations/add_maint_facility_details.sql
sql/migrations/add_maint_settings.sql
sql/migrations/integrate_maint_settings_behavior.sql
sql/migrations/add_maint_preventive_auto_wo.sql
sql/migrations/add_maint_daily_ops_view.sql
sql/migrations/extend_maint_cmms_daily_summary.sql

# Projektni biro — pb_tasks, pb_work_reports, load RPC
sql/migrations/add_pb_module.sql
sql/migrations/pb_load_stats_mechanical_engineering.sql
sql/migrations/pb_mechanical_engineers_rpc.sql
sql/migrations/add_pb_notifications.sql
sql/migrations/add_pb4_rls_and_agg.sql
sql/migrations/fix_pb_tasks_soft_delete_rls.sql
sql/migrations/add_pb_soft_delete_rpc.sql

# Faza 1 security hardening (2026-04-23). Migracija je idempotentna i
# preživljava izvrsavanje cak i ako v_production_operations ne postoji
# (REVOKE FROM anon na nepostojecem objektu padne — pa je u CI svjesno
# preskocena dok ne ukljucimo full plan-proizvodnje migracije u CI listu).
# sql/migrations/revoke_anon_v_production_operations.sql

# Lokacije × Mašine — Faza 1 (2026-05-17): MACHINE enum + seed mašina
# iz bigtehn_machines_cache; source kolona na movements; view+RPC samo ako
# postoje PP cache tabele (vidi ispod).
# `add_loc_machine_enum.sql` MORA biti odvojena migracija od seed-a — PG
# ne dozvoljava korišćenje nove enum vrednosti u istoj tx u kojoj je dodata.
sql/migrations/add_loc_machine_enum.sql
sql/migrations/add_loc_machine_locations.sql
# Dopuna seed-a iz maint_machines (lokalni autoritativni katalog) — pokriva i
# mašine koje su u BigTehn-u flagovane no_procedure=TRUE (CAM stanice 17.x).
sql/migrations/add_loc_machine_locations_from_maint.sql
sql/migrations/add_loc_movement_source_column.sql
# `add_loc_bigtehn_op_status_rpc.sql` zavisi od `bigtehn_work_order_lines_cache`,
# `bigtehn_tech_routing_cache`, `production_overlays` — u plain CI bootstrap-u
# nisu stubovani (samo je `bigtehn_machines_cache` minimalni stub). Produkcija
# Supabase: primeni ručno kao treći korak.
# sql/migrations/add_loc_bigtehn_op_status_rpc.sql

# Faza 2A — BigTehn ingest worker u DRY-RUN modu (skenira nove prijave,
# loguje šta bi uradio bez side-effect-a). Zavisi od bigtehn_tech_routing_cache
# pa je preskočeno u CI; primeni ručno na Supabase posle Faza 1 migracija.
# sql/migrations/add_loc_phase2a_bigtehn_ingest_dryrun.sql

# Faza 2B — ARMED grana worker-a (INITIAL=RN total, TRANSFER=full placement qty),
# trigger filter za source='bigtehn' (loop prevention), 7d safety net.
# Posle deploy-a: SELECT loc_bigtehn_ingest_arm(TRUE) da se aktivira. Preskočeno u CI.
# sql/migrations/add_loc_phase2b_bigtehn_ingest_armed.sql

# Faza 2B refinement — smart ident_broj parser. Longest active predmet match
# protiv bigtehn_items_cache: „9400/1/165" → predmet=„9400/1" tp=„165" ako je
# „9400/1" aktivan predmet. Inače fallback na (split 1 / split 2).
# sql/migrations/add_loc_phase2b_smart_ident_parser.sql

# Faza 2B parser dopuna — dash konvencija (revizija/sub-batch). „9400-1/430"
# → predmet=„9400", tp=„1/430" (Pass 2 grana). Idempotent CREATE OR REPLACE.
# sql/migrations/add_loc_phase2b_smart_ident_parser_dash.sql

# Faza 2C — admin status RPC + manual trigger za UI panel (Sync tab).
# Zavisi od Faza 2A state tabele + loc_is_admin() + loc_sync_worker_heartbeat.
# Preskočeno u CI jer zavisi od Faza 2A worker funkcije; primeni ručno.
# sql/migrations/add_loc_bigtehn_ingest_status_rpc.sql
